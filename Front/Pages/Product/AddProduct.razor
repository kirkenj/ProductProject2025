@inject Front.Services.CustomAuthStateProvider stateProvider;
@inject NavigationManager navManager
@page "/AddProduct"

@if (stateProvider.CurrentUser == null)
{
    navManager.NavigateTo("Products");
}

<Front.Components.ProductService.ProductAdd/>